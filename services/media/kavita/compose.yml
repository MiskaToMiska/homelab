services:
  kavita:
    image: lscr.io/linuxserver/kavita:latest
    container_name: kavita
    environment:
     - PUID=${PUID}
     - PGID=${PGID}
     - TZ=${TZ}
    volumes:
     - ${APPDATA_DIR}/kavita/config:/config:Z
     - ${MEDIA_DIR}/books:/data/books:ro,z
     - ${MEDIA_DIR}/comics:/data/comics:ro,z
    ports:
     - "5000:5000"
    restart: always
